{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "plugins",
    "examples"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna link",
    "start:plugins": "cd plugins && yarn run build",
    "start:examples": "yarn workspace examples start"
  }
}
